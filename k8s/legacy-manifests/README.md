# Legacy static manifests

These manifests were used for direct `kubectl apply` deployment before the
project migrated to a parametrized Helm chart (`k8s/homelab-siem-chart/`).

Kept here for historical reference and documentation of the project's
evolution. **Do not apply these directly** — they are superseded by the
Helm chart and will conflict with resources it manages.
